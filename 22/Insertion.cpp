// unsolved 



#include <iostream>
#include <algorithm>
#include <vector>
#include <cmath>
using namespace std;
typedef int KeyType;

int n, shifted[50000];
int A[50000];
void solve(){
	Node* candidates = NULL;
	
}
int main(){
	ios_base::sync_with_stdio(false); 
	cin.tie(NULL);
	cout.tie(NULL);
	int test;
	cin>>test;
	while(test--){
		int num;
		cin>>num;
		for(int i=0;i<num;i++){
			cin>>shifted[i];
		}
	}
	
	return 0;
}
